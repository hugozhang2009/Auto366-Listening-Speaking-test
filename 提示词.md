用Python写一个天学网自动化口语答题程序，运行后弹出窗口上方是一个地址栏，用户可以自行粘贴文件夹路径的地址，文件夹名必须为questions。如果用户选择的文件夹不符合要求在地址栏中用红色的字体选项显示请重新选择文件夹。地址栏右侧有一个浏览按钮点击后打开文件资源管理器文件夹选择页面让用户选择文件夹同时在地址栏里显示已选择的文件夹路径。地址栏下方是一个固定音频路径框，用户可以手动选择固定音频所在目录，右侧有浏览按钮。固定音频路径框下方是一个视频播放窗口。当用户选择好了名为questions的文件夹之后，找到同时含有1-ZC.mp3文件和mp4文件的名为media的目录，并将该目录中的mp4文件在视频播放窗口中打开，进行静音播放。下方提示"请根据part a内容确认是否为本套试题答案"再下方有一个"启动自动化听说"的按钮点击后开始执行程序核心逻辑。启动后视频播放窗口消失，出现一个日志输出框，其中的内容均以当前标准时间为开头，如16:15.000。日志框下方有暂停/继续按钮和退出按钮。接着启动一个与1welcome.mp3时长相同的倒计时，日志框中显示"正在等待播放欢迎语，请勿点击跳过。"同时实时更新输出倒计时剩余秒数，注意不要刷屏，在一条日志中进行显示。倒计时结束后启动第二个倒计时，时长与2partazhishi1.mp3相同，日志框中显示"正在等待播放PartA介绍语"，其余要求同上。

---

接着读取用户所选 `questions` 文件夹的路径，按以下步骤构建完整程序逻辑：

---

### 一、固定音频路径推导

- 优先使用用户手动选择的固定音频文件夹路径。
- 若用户未选择，则从用户选择的文件夹路径（例如 `E:\Up366StudentFiles\flipbooks\4A43FDC0B7CE8701415B6E28D0B37B23\questions`）中提取其父目录，拼接 `bookres\media\` 得到固定音频所在目录，例如 `E:\...\4A43FDC0B7CE8701415B6E28D0B37B23\bookres\media\`。
- 该目录下应包含以下固定音频文件（文件名与流程中完全一致）：
  - `1welcome.mp3`
  - `2partazhishi1.mp3`
  - `3parta1min.mp3`
  - `4parta1again.mp3`
  - `5parta1read.mp3`
  - `beep.mp3`
  - `6partbzhishi.mp3`
  - `7partbsanwen.mp3`
  - `9partbwuda.mp3`（五答介绍语，流程中第17步及五答循环前只需播放一次）
  - `10partczhishi.mp3`
  - `11partcretell.mp3`（PartC提示音）
- 程序启动后，**立即**读取这些固定音频文件的时长（使用 `mutagen` 或 `pydub` 等库），并存储在变量中供后续倒计时使用。若文件不存在，在日志框报错并弹出文件资源管理器文件夹选择页面让用户选择含有上述文件的文件夹。

---

### 二、扫描并分类 `questions` 下的子文件夹

- 遍历用户所选 `questions` 目录下的所有一级子文件夹（名称随机，如 `0B41A...`、`40D11...` 等）。
- 对每个子文件夹，检查其下的 `media` 子目录，读取其中的所有音频/视频文件。
- **类型识别规则**（优先级从高到低）：
  1. **PartA**：若 `media` 中同时存在 `.mp4` 文件和 `1-ZC.mp3` 文件，则判定为 PartA（只应有一个这样的文件夹）。
  2. **PartC**：若 `media` 中存在文件名包含 `partc`（不区分大小写）的 `.mp3` 文件（如 `partc-ZC.mp3`），则判定为 PartC（只应有一个）。
  3. **三问（PartB 三问）**：若 `media` 中存在文件名以 `2.` 开头的 `.mp3` 文件（如 `2.1-ZC.mp3`、`2.1.1.mp3`），则判定为三问题目。根据 `2.X` 中的 `X`（1、2、3）确定题号，收集所有此类文件夹，按题号升序排列。
  4. **五答（PartB 五答）**：若 `media` 中存在文件名以 `3.` 开头的 `.mp3` 文件（如 `3.1-ZC.mp3`、`3.1.1.mp3`），则判定为五答题目。根据 `3.X` 中的 `X`（1~5）确定题号，收集所有此类文件夹，按题号升序排列。
- 若某个题型缺少对应数量的文件夹（例如三问不足3个或五答不足5个），则在日志中警告但尽量继续（按实际数量执行，流程中循环次数自适应）。
- 对于每个题型，记录其 `media` 路径、题号，以及文件列表。

---

### 三、各题型内部文件解析规则

#### 1. PartA

- `media` 中应有一个 `.mp4` 文件（视频原文）和一个 `1-ZC.mp3`（答案音频）。
- 视频文件路径用于后续播放，答案音频用于最后播放。

#### 2. PartB 三问（题号 2.1、2.2、2.3）

- 每个题目的文件夹中：
  - **作者回答**：文件名匹配 `2.X-ZC.mp3`（`X` 为题号）的音频文件（只有一个）。
  - **答案列表**：文件名以 `2.X.` 开头且**不是** `2.X-ZC.mp3` 的所有 `.mp3` 文件（如 `2.1.1.mp3`、`2.1.2.mp3`）。将这些文件按名称升序排序，作为该题的可选答案。
- 若答案列表为空，报错并跳过该题。

#### 3. PartB 五答（题号 3.1 ~ 3.5）

- 每个题目的文件夹中：
  - **作者问题**：文件名**不**以 `3.X.` 开头（即不是答案文件），且**不**包含 `part`、`ZC` 等固定标识的 `.mp3` 文件（通常是一个哈希字符串命名的文件，如 `C45E1F...mp3`）。但为了鲁棒，也可以采用排除法：在 `media` 中排除所有答案文件（以 `3.X.` 开头的）和已知固定文件（如 `3.X-ZC.mp3` 如果存在），剩余的唯一 `.mp3` 视为作者问题。若仍有多个，则选择文件名最短或第一个。
  - **答案列表**：文件名以 `3.X.` 开头的所有 `.mp3` 文件（如 `3.1.1.mp3`、`3.1.2.mp3`），按升序排序。
- **注意**：`3.X-ZC.mp3` 文件（如 `3.1-ZC.mp3`）在五答中可能不存在或作为额外文件，程序应忽略它（除非它被确认为唯一的问题文件，但根据样例，作者问题是哈希文件，因此 `3.X-ZC.mp3` 可忽略）。实现时，优先采用"不是答案文件且不是固定音频"的规则。

#### 4. PartC

- `media` 中应有一个 `partc-ZC.mp3` 或类似名称（包含 `partc`）的音频文件作为答案原文。记录其路径。

---

### 四、主流程控制（按 `流程.txt` 顺序）

- 程序启动后，**隐藏视频播放窗口**，显示日志输出框。日志框以 `HH:MM.ss` 格式（如 `16:15.00`）为每条日志的时间戳。
- 日志框下方有暂停/继续按钮和退出按钮，用户可以随时暂停或继续程序。

- 所有倒计时均在同一行实时更新剩余秒数（使用 `\r` 刷新），不换行刷屏。示例日志：
  
  ```
  16:15.000 正在等待播放欢迎语，剩余 3 秒
  ```
  
  秒数递减至0时，该行变为"已完成"并换行输出下一阶段日志。

#### 阶段1：欢迎语

- 时长 = `1welcome.mp3` 的时长（秒，取整或保留小数，显示整数秒即可）。
- 日志显示"正在等待播放欢迎语，请勿点击跳过。"并实时更新剩余秒数。
- 倒计时结束后，日志输出"欢迎语播放完成"。

#### 阶段2：PartA 介绍语

- 时长 = `2partazhishi1.mp3` 的时长。
- 日志显示"正在等待播放PartA介绍语"，倒计时。

#### 阶段3：PartA 视频播放（静音）

- 使用视频播放窗口（此时应重新显示）播放 PartA 的 `.mp4` 文件，**强制静音**。
- 记录视频时长（通过 `ffmpeg` 或 `opencv` 获取），播放期间倒计时，需在日志中输出"正在播放PartA视频（静音），时长 XX 秒"，倒计时。
- 视频播放完毕后，关闭视频播放窗口（隐藏）。

#### 阶段4：PartA 1分钟准备

- 时长固定 60 秒（流程第5步），日志显示"正在等待1分钟准备"，倒计时。

#### 阶段5：再听一遍原文提示

- 时长 = `4parta1again.mp3` 的时长。
- 日志显示"正在等待再听一遍原文提示"，倒计时。

#### 阶段6：5秒准备（再次播放原文前）

- 时长固定 5 秒，日志显示"等待5秒准备"，倒计时。

#### 阶段7：再次播放 PartA 原文

- **再次**播放同一个 `.mp4` 视频文件（静音），记录时长并输出日志，倒计时。
- 播放完毕后隐藏视频窗口。

#### 阶段8：现在开始录音提示

- 时长 = `5parta1read.mp3` 的时长。
- 日志显示"正在等待播放录音提示"，倒计时。

#### 阶段9：5秒准备（录音前）

- 固定 5 秒，日志显示"等待5秒准备"，倒计时。

#### 阶段10：beep 提示音（静音等待）

- 静音等待 `beep.mp3` 时长，日志显示"正在等待播放提示音"。

#### 阶段11：播放 PartA 答案

- 播放 `1-ZC.mp3` 音频文件（非静音），记录其时长。
- 播放期间日志显示"正在播放PartA答案"，倒计时，播放完毕后日志输出"PartA答案播放完成"。

#### 阶段12：PartB 介绍语

- 时长 = `6partbzhishi.mp3`。
- 日志显示"正在等待播放PartB介绍语"，倒计时。

#### 阶段13：3秒准备（PartB 视频前）

- 固定 3 秒，日志显示"等待3秒准备"，倒计时。

#### 阶段14：PartB 视频播放

- 注意：流程中第15步"等待播放PartB视频" – 但根据 `tree.txt`，PartB 视频位于哪个文件夹？在 `424A...` 中有 `121E99...mp4` 标注为 PartB 视频，可能对应三问部分。但流程中 PartB 包含三问和五答，可能视频只有一个，播放一次。程序需从所有子文件夹中查找包含 `.mp4` 且文件名包含 `PartB` 或位于三问第一问文件夹的视频文件（实际样例中三问第一问文件夹内有 PartB 视频）。因此，程序应扫描所有子文件夹，找到任一个 `.mp4` 文件（排除 PartA 的），若存在则作为 PartB 视频播放（静音）。
- 播放视频，时长由视频决定，输出日志"正在播放PartB视频（静音）"，倒计时。

#### 阶段15：三问提示

- 时长 = `7partbsanwen.mp3`。
- 日志显示"正在等待播放三问提示"，倒计时。

#### 阶段16：三问循环（按题号升序，每个问题重复以下步骤）

- **对于每个三问题目（2.1、2.2、2.3）**：
  1. **等待20秒准备**（固定20秒），日志显示"正在等待第 X 问准备（20秒）"，倒计时。
  2. **静音等待 beep**，日志显示"正在等待播放提示音"。
  3. **播放当问答案**：从该题的答案列表中**随机选取一个**（或始终选第一个，推荐随机以增加变化），播放该音频文件，记录其时长 `t_answer`。
     - 播放期间日志显示"正在播放第 X 问答案"，倒计时。
     - 播放完毕后，计算剩余等待时间 = `10 - t_answer`（若 t_answer > 10 则剩余为0），然后等待剩余秒数，日志显示"等待补齐回答时间（X秒）"，倒计时。
  4. **静音等待作者回答**：静音等待 `2.X-ZC.mp3` 文件时长，日志显示"正在等待播放第 X 问作者回答"。
  5. **等待2秒**（固定），日志显示"等待2秒准备"，倒计时。
  6. **再次静音等待作者回答**：静音等待同一个作者回答音频时长，日志显示"正在等待再次播放第 X 问作者回答"。
- 每个问题结束后，日志输出"第 X 问完成"。

#### 阶段17：五答介绍语

- 时长 = `9partbwuda.mp3`（注意流程中第17步是播放介绍语，然后五答循环，但循环内第一步也写了播放介绍语，实际只需一次）。
- 日志显示"正在等待播放五答介绍语"，倒计时。

#### 阶段18：五答循环（按题号升序，每个问题重复以下步骤）

- **对于每个五答题目（3.1 ~ 3.5）**：
  1. **静音等待问题**：静音等待作者问题音频时长，日志显示"正在等待播放第 X 问问题"。
  2. **等待2秒**（固定），日志显示"等待2秒准备"，倒计时。
  3. **再次静音等待问题**：静音等待同一个问题音频时长，日志显示"正在等待再次播放第 X 问问题"。
  4. **等待10秒准备**（固定），日志显示"等待10秒准备"，倒计时。
  5. **静音等待 beep**，日志显示"正在等待播放提示音"。
  6. **播放当问答案**：从该题的答案列表中随机选取一个（或第一个），播放，记录时长 `t_answer`。
     - 播放期间日志显示"正在播放第 X 问答案"，倒计时。
     - 播放完毕后，等待 `10 - t_answer` 秒（若为负则0），日志显示"等待补齐回答时间"，倒计时。
- 每个问题结束后，日志输出"第 X 问完成"。

#### 阶段19：PartC 介绍语

- 时长 = `10partczhishi.mp3`。
- 日志显示"正在等待播放PartC介绍语"，倒计时。

#### 阶段20：15秒准备

- 固定15秒，日志显示"等待15秒准备"，倒计时。

#### 阶段21：播放 PartC 原文（第一次，静音等待）

- 静音等待 `partc-ZC.mp3` 或相关音频文件时长，日志显示"正在等待播放PartC原文（第一次）"。

#### 阶段22：2秒准备

- 固定2秒，日志显示"等待2秒准备"，倒计时。

#### 阶段23：播放 PartC 原文（第二次，静音等待）

- 静音等待同一个 PartC 音频时长，日志显示"正在等待播放PartC原文（第二次）"。

#### 阶段24：60秒准备

- 固定60秒，日志显示"等待60秒准备"，倒计时。

#### 阶段25：等待播放PartC提示音（静音等待）

- 时长 = `11partcretell.mp3` 的时长。
- 静音等待，日志显示"正在等待播放PartC提示音"。

#### 阶段26：播放 beep 提示音

- 播放 `beep.mp3`，日志输出"播放提示音"。

#### 阶段27：播放 PartC 答案

- 播放 `partc-ZC.mp3` 音频文件（非静音），记录其时长。
- 播放期间日志显示"正在播放PartC答案"，倒计时，播放完毕后日志输出"PartC答案播放完成"。

#### 阶段28：流程结束

- 日志输出"所有流程执行完毕，程序结束"，并保持日志框可见，不自动关闭。同时提供"退出"按钮或等待用户手动关闭。

---

### 五、多答案选取策略细化

- 对于三问和五答，每个题目都有多个答案文件（如 `.1`、`.2` 等）。程序应**随机**选择一个播放，以增加每次运行的变化。
- 若答案列表为空，则跳过该题并输出错误日志。
- 随机选取可使用 `random.choice`，确保每次运行结果不同。

---

### 六、异常处理

- 若固定音频文件缺失，弹窗报错并终止。
- 若 `questions` 下缺少某些题型或题号，在日志中输出警告，但继续执行已有部分（例如只有两问三问，则只循环两次）。
- 若某个音频文件无法播放（如损坏），捕获异常并继续下一个文件。

---

### 七、界面交互细节（补充）

- 地址栏中的路径校验：文件夹名必须为 `questions`。若不满足，地址栏文字变红并显示"请重新选择文件夹"。
- 固定音频路径框：用户可手动输入或点击浏览按钮选择固定音频所在目录（media目录）。
- 浏览按钮调用 `tkinter.filedialog.askdirectory`，选中后自动校验。
- 视频播放窗口可使用 `tkinter` 内嵌 `opencv` 或 `vlc` 控件，静音播放。
- "启动自动化听说"按钮点击后，隐藏视频窗口和地址栏等，只显示日志输出框（或置于顶层），开始执行流程。
- 暂停/继续按钮：位于日志框下方，启动后变为可点击状态，点击可暂停/继续程序执行。
- 所有倒计时的显示使用 `after` 方法定时刷新，避免阻塞主线程。
- 暂停功能：暂停期间，倒计时和静音等待都会暂停，音频播放不受影响。
